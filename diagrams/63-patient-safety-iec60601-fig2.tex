% Implant screening workflow (flowchart) for MR Conditional / Safe / Unsafe devices.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small, node distance=8mm and 14mm,
    box/.style={rectangle, rounded corners, draw, thick, align=center,
                fill=blue!6, text width=34mm, minimum height=9mm},
    dec/.style={diamond, aspect=2, draw, thick, align=center,
                fill=orange!12, inner sep=1pt, text width=28mm},
    term/.style={rectangle, draw, thick, align=center, text width=34mm,
                minimum height=9mm},
    bad/.style={term, fill=red!12},
    good/.style={term, fill=green!12},
    flow/.style={->, thick},
  ]

  \node[box] (id) {شناسایی دقیق ایمپلنت\\(مدل، سازنده، تاریخ)};
  \node[dec, below=of id] (label) {برچسب MR موجود است؟};
  \node[box, right=26mm of label] (check) {بررسی سازنده /\\MRIsafety.com\\(در صورت ابهام: اسکن نکنید)};
  \node[dec, below=14mm of label] (class) {رده؟};

  \node[bad, left=of class] (unsafe) {ناایمن:\\اسکن نکنید\\(یا روش جایگزین)};
  \node[good, below left=14mm and 2mm of class] (safe) {ایمن:\\اسکن با پروتکل روتین};

  \node[dec, below=22mm of class] (cond) {مشروط:\\همهٔ شرایط برچسب\\برآورده شده؟};
  \node[good, below left=12mm and 2mm of cond] (scanlim)
        {اسکن با محدودیت\\\RL{(محدودسازی \LR{$B_0$/SAR/slew}،}\\\RL{حالت عادی، پایش)}};
  \node[bad, below right=12mm and 2mm of cond] (noscan) {اسکن نکنید};

  % edges
  \draw[flow] (id) -- (label);
  \draw[flow] (label) -- node[above]{خیر} (check);
  \draw[flow] (label) -- node[right]{بله} (class);
  \draw[flow] (class) -- node[above]{ناایمن} (unsafe);
  \draw[flow] (class) -- node[above left, pos=0.6]{ایمن} (safe);
  \draw[flow] (class) -- node[right]{مشروط} (cond);
  \draw[flow] (cond) -- node[above left]{بله} (scanlim);
  \draw[flow] (cond) -- node[above right]{خیر} (noscan);

\end{tikzpicture}}
\end{document}
