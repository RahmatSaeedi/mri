% TEM resonator structure: an outer cylindrical RF shield, independent
% transmission-line elements (rungs) each end-loaded with a tuning capacitor,
% with no continuous end-ring; sample region in the center.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small]

  % --- outer RF shield (cylinder, drawn as a rectangle with end ellipses) ---
  \draw[thick, blue!55!black] (0,0) -- (8,0);
  \draw[thick, blue!55!black] (0,3.4) -- (8,3.4);
  \draw[thick, blue!55!black] (0,0) arc (-90:90:0.45 and 1.7);
  \draw[thick, blue!55!black, dashed] (0,0) arc (270:90:0.45 and 1.7);
  \draw[thick, blue!55!black] (8,0) arc (-90:90:0.45 and 1.7);
  \node[blue!55!black, font=\footnotesize, anchor=south] at (4,3.5) {پوششِ بیرونیِ RF (استوانهٔ رسانا)};

  % --- TEM elements (rungs) as vertical line-segments inside, near the wall ---
  \foreach \x in {1.2,2.4,3.6,4.8,6.0,7.0} {
    % rung as a thick conductor
    \draw[very thick] (\x,0.75) -- (\x,2.65);
    % tuning capacitor at the bottom end of each element
    \draw[thick] (\x,0.75) -- (\x,0.62);
    \draw[thick] (\x-0.14,0.62) -- (\x+0.14,0.62);
    \draw[thick] (\x-0.14,0.5) -- (\x+0.14,0.5);
    \draw[thick] (\x,0.5) -- (\x,0.37);
    % connect cap bottom to shield base rail
    \draw[thick] (\x,0.37) -- (\x,0.1);
  }
  \node[font=\footnotesize, anchor=west] at (0.05,2.95) {عناصرِ TEM (میله = خط انتقال)};
  \draw[->] (0.05,2.85) -- (1.2,2.55);

  \node[font=\footnotesize] at (4.0,-0.45) {$\equiv$ خازنِ تنظیمِ $C$ در انتهای هر عنصر};

  % --- sample region in the center ---
  \draw[dashed, gray] (4,1.7) ellipse (1.6 and 0.7);
  \node[gray!50!black, font=\footnotesize] at (4,1.7) {ناحیهٔ نمونه};

  % note: no continuous end-ring
  \node[font=\footnotesize, red!65!black] at (4.0,-0.95)
    {بدون حلقهٔ انتهاییِ پیوسته $\Rightarrow$ هر عنصر مستقلاً قابل‌تنظیم (مناسب pTx)};

\end{tikzpicture}}
\end{document}
