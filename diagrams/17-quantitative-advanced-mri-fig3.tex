% BOLD causal chain: neural activity -> O2 consumption -> overshoot CBF -> less deoxyHb -> field homogeneity up -> T2* up -> signal up.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small, node distance=7mm and 8mm]
  \tikzset{
    box/.style={rounded corners=2pt, draw=black, align=center, text width=27mm,
                minimum height=13mm, inner sep=2pt},
    phys/.style={box, fill=blue!8},
    sig/.style={box, fill=green!10},
    note/.style={align=center, font=\footnotesize\itshape, text=black!60, text width=27mm}
  }

  % --- top row ---
  \node[phys] (a) {فعالیت عصبی (شلیک نورون‌ها)};
  \node[phys, right=of a] (b) {افزایش مصرف موضعی $\mathrm{O_2}$ \RL{($\mathrm{CMRO_2}$)}};
  \node[phys, right=of b] (c) {جهش جریان خون \RL{($\mathrm{CBF}\uparrow\uparrow$)}};

  \draw[->, very thick] (a) -- (b);
  \draw[->, very thick] (b) -- (c);

  % --- turn down ---
  \node[phys, below=10mm of c] (d) {کاهش کسر deoxyHb (شست‌وشو)};
  \draw[->, very thick] (c) -- (d);

  % --- bottom row (right to left) ---
  \node[phys, left=of d] (e) {همگن‌تر شدن میدان موضعی (کاهش پذیرفتاری)};
  \node[sig, left=of e] (f) {$T_2^{*}\uparrow$ $\Rightarrow$ افزایش سیگنال};

  \draw[->, very thick] (d) -- (e);
  \draw[->, very thick] (e) -- (f);

  % --- key annotation centred below the whole bottom row ---
  \node[font=\footnotesize, text=black!55, below=6mm of f.south east, anchor=north, align=center, xshift=15mm]
       {نکته: افزایش CBF بیش از $\mathrm{CMRO_2}$ است، پس deoxyHb عملاً افت می‌کند};
\end{tikzpicture}}
\end{document}
