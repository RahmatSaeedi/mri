% MRI data pipeline: ADC -> hardware buffer (hard real-time) -> host buffer -> recon -> DICOM (soft real-time).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    blk/.style={draw, rounded corners=2pt, align=center, minimum height=12mm, minimum width=22mm, fill=blue!6},
    soft/.style={draw, rounded corners=2pt, align=center, minimum height=12mm, minimum width=22mm, fill=green!8},
    >=Stealth]

  % --- Hard real-time chain (top row) ---
  \node[blk] (coil) {سیم‌پیچ گیرنده\\(آنالوگ)};
  \node[blk, right=8mm of coil] (lna) {LNA + mixer\\(پایین‌تبدیل)};
  \node[blk, right=8mm of lna] (adc) {ADC + DDC\\(نمونه‌بردار I/Q)};
  \node[blk, right=8mm of adc] (fifo) {بافر سخت‌افزاری\\FIFO (FPGA)};

  \draw[->, thick] (coil) -- (lna);
  \draw[->, thick] (lna) -- (adc);
  \draw[->, thick] (adc) -- (fifo);

  % --- Soft real-time chain (bottom row) ---
  \node[soft, below=22mm of fifo] (hostbuf) {بافر میزبان\\(RAM، حلقوی)};
  \node[soft, left=8mm of hostbuf] (assemble) {مونتاژ\\فریم\\+ فراداده};
  \node[soft, left=8mm of assemble] (recon) {بازسازی\\(FFT / شبکه‌بندی)};
  \node[soft, left=8mm of recon] (dicom) {تصویر DICOM\\$\rightarrow$ PACS};

  \draw[->, thick] (fifo) -- node[right, font=\scriptsize] {DMA / پیوند پرسرعت} (hostbuf);
  \draw[->, thick] (hostbuf) -- (assemble);
  \draw[->, thick] (assemble) -- (recon);
  \draw[->, thick] (recon) -- (dicom);

  % --- Domain labels / boxes ---
  \begin{scope}[on background layer]
    \node[draw=red!55, dashed, rounded corners, fit=(coil)(lna)(adc)(fifo),
          inner sep=5mm, label={[red!60!black]above:بی‌درنگ سخت (FPGA / گیرندهٔ دیجیتال)}] {};
    \node[draw=green!50!black, dashed, rounded corners, fit=(dicom)(recon)(assemble)(hostbuf),
          inner sep=5mm, label={[green!40!black]below:بی‌درنگ نرم (میزبان / Linux)}] {};
  \end{scope}

\end{tikzpicture}}
\end{document}
