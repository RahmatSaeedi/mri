% GPU memory hierarchy: global/HBM (large, slow) -> L2 (shared) -> per-SM L1/shared + registers (fast).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    lvl/.style={rounded corners=3pt, draw=black, very thick, minimum width=8.6cm, align=center, inner sep=6pt}]

  % Global / HBM
  \node[lvl, fill=red!12] (glob) at (0,0)
    {\textbf{حافظهٔ سراسری (HBM)} $\;\sim 1.5$ TB/s\\[1pt]
     \footnotesize بزرگ $\cdot$ نهفتگی بالا $\cdot$ کند};

  % L2
  \node[lvl, fill=orange!14, below=1.1cm of glob, minimum width=7.4cm] (l2)
    {\textbf{حافظهٔ نهان L2}\\[1pt]\footnotesize مشترک میان همهٔ SMها};

  % per-SM L1 / shared / registers
  \node[lvl, fill=green!14, below=1.1cm of l2, minimum width=6.2cm] (sm)
    {\textbf{هر SM: L1 / حافظهٔ مشترک} (ده‌ها KB)\\[1pt]
     \textbf{ثبات‌ها} (سریع‌ترین، خصوصیِ هر نخ)};

  % vertical bus arrows
  \draw[<->, very thick] (glob) -- (l2)
    node[midway, right=2pt, align=left, font=\footnotesize]{هم‌آمیزیِ دسترسی\\حیاتی است};
  \draw[<->, very thick] (l2) -- (sm);

  % speed / size annotation on the right
  \draw[->, thick, gray] ($(sm.south east)+(1.5,-0.1)$) -- ($(glob.north east)+(1.5,0.1)$)
    node[midway, right, align=left, font=\footnotesize, black]{بزرگ‌تر\\$+$ کندتر};
  \draw[->, thick, gray] ($(glob.north east)+(2.6,0.1)$) -- ($(sm.south east)+(2.6,-0.1)$)
    node[midway, right, align=left, font=\footnotesize, black]{سریع‌تر\\$+$ نزدیک‌تر};

\end{tikzpicture}}
\end{document}
