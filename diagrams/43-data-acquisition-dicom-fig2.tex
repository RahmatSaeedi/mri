% Double (ping-pong) buffering: hardware fills one block while software processes the other; roles swap.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    blk/.style={draw, rounded corners=2pt, align=center, minimum height=11mm, minimum width=24mm},
    hw/.style={blk, fill=blue!8},
    sw/.style={blk, fill=green!8},
    role/.style={font=\footnotesize}]

  % ---- State t0 ----
  \node[role] (t0) {$t_0$};
  \node[hw, right=10mm of t0] (a0) {بلوک A\\(در حال پرشدن)};
  \node[sw, right=28mm of a0] (b0) {بلوک B\\(در حال پردازش)};
  \node[role, above=2mm of a0] {سخت‌افزار $\rightarrow$};
  \node[role, above=2mm of b0] {نرم‌افزار $\rightarrow$};

  % ---- swap label ----
  \node[below=7mm of $(a0)!0.5!(b0)$, font=\footnotesize\itshape] (swap) {تعویض هنگام پرشدن A};
  \draw[->, gray, thick] ($(a0.south)+(0,-2mm)$) -- ++(0,-9mm);
  \draw[->, gray, thick] ($(b0.south)+(0,-2mm)$) -- ++(0,-9mm);

  % ---- State t1 ----
  \node[role, below=22mm of t0] (t1) {$t_1$};
  \node[sw, right=10mm of t1] (a1) {بلوک A\\(در حال پردازش)};
  \node[hw, right=28mm of a1] (b1) {بلوک B\\(در حال پرشدن)};
  \node[role, below=2mm of a1] {$\leftarrow$ نرم‌افزار};
  \node[role, below=2mm of b1] {$\leftarrow$ سخت‌افزار};

  % ---- Correctness note ----
  \node[draw=red!50, dashed, rounded corners, align=center, font=\footnotesize,
        below=12mm of $(a1)!0.5!(b1)$]
        {درستی: $T_{\mathrm{proc}} \le B/\lambda$\\(زمان پرشدن یک بلوک $\ge$ زمان پردازش بلوک دیگر)};

\end{tikzpicture}}
\end{document}
