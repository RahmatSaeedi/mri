% Eight-step corrective-action workflow for MRI repair, with decision gateways
% (immediate safety, KB hit, part availability, re-verification pass, CAPA).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small, node distance=6mm,
    wfbox/.style={rectangle, rounded corners=3pt, draw=black!70, very thick,
      text width=8.2cm, align=center, inner sep=6pt, minimum height=1.1cm},
    main/.style={->, very thick, black!60},
    branch/.style={->, thick, red!65!black, dashed},
    btag/.style={font=\footnotesize, color=red!65!black, fill=white, inner sep=1.5pt},
    sidebox/.style={rectangle, rounded corners=2pt, draw=red!55!black, thick,
      fill=red!8, text width=4.3cm, align=center, inner sep=4pt, font=\footnotesize}]

  \node[wfbox, fill=blue!10] (s1)
    {\textbf{۱. پذیرش و ثبت}\\تیکت، نشانه، فوریت، پرچم ایمنیِ فوری};
  \node[wfbox, fill=red!12, below=of s1] (s2)
    {\textbf{۲. تریاژ و پایدارسازیِ ایمنیِ اولیه}\\خطر فوری؟ $\to$ حالت ایمن / تخلیه / قرنطینه};
  \node[wfbox, fill=orange!12, below=of s2] (s3)
    {\textbf{۳. عیب‌یابی}\;\RL{(فصل ۷۰: درخت خطا، نشانه$\to$علت)}\\جست‌وجوی پایگاه دانش با کدهای لاگ و کلیدواژه‌ها};
  \node[wfbox, fill=orange!12, below=of s3] (s4)
    {\textbf{۴. تحلیل علت ریشه‌ای (RCA)}\\پنج‌چرا / ایشیکاوا / تحلیل درخت خطا};
  \node[wfbox, fill=yellow!18, below=of s4] (s5)
    {\textbf{۵. برنامه‌ریزی تعمیر}\\قطعات، ابزار، MTTR، سطح مهارت، بازهٔ زمانی};
  \node[wfbox, fill=green!12, below=of s5] (s6)
    {\textbf{۶. اجرای تعمیر}\\رویهٔ گام‌به‌گام + ثبتِ هر گام};
  \node[wfbox, fill=green!12, below=of s6] (s7)
    {\textbf{۷. بازراستی‌آزماییِ پس از تعمیر}\\آزمونِ پذیرش/ردّ، QA مختصر};
  \node[wfbox, fill=blue!12, below=of s7] (s8)
    {\textbf{۸. اختتام + به‌روزرسانی پایگاه دانش + ارزیابی CAPA}\\الگوی تکرارشونده؟ $\to$ گشودن CAPA (فصل ۵۶)};

  % main downward flow
  \draw[main] (s1) -- (s2);
  \draw[main] (s2) -- (s3);
  \draw[main] (s3) -- (s4);
  \draw[main] (s4) -- (s5);
  \draw[main] (s5) -- (s6);
  \draw[main] (s6) -- (s7);
  \draw[main] (s7) -- (s8);

  % side decision boxes (right)
  \node[sidebox, right=1.0cm of s2] (b2) {حالت ایمن / تخلیه / قرنطینهٔ دستگاه};
  \node[sidebox, right=1.0cm of s5] (b5) {راه‌حل موقت + سفارش قطعه (فصل ۷۲)};

  \draw[branch] (s2.east) -- node[btag, above]{خطر؟\,بله} (b2.west);
  \draw[branch] (s5.east) -- node[btag, above]{قطعه موجود است؟\,خیر} (b5.west);

  % KB hit: skip RCA, go straight to step 5 (left fast path)
  \coordinate (kb1) at ($(s3.west)+(-1.0,0)$);
  \coordinate (kb2) at ($(s5.west)+(-1.0,0)$);
  \draw[branch] (s3.west) -- (kb1) -- (kb2)
     node[btag, midway, rotate=90, anchor=south]{مدخل پایگاه دانش یافت شد $\Rightarrow$ پرش به گام ۵} -- (s5.west);

  % re-verify reject: loop back to step 3 (left)
  \coordinate (rv1) at ($(s7.west)+(-1.6,0)$);
  \coordinate (rv2) at ($(s3.west)+(-1.6,0)$);
  \draw[branch] (s7.west) -- (rv1) -- (rv2)
     node[btag, midway, rotate=90, anchor=south]{شکستِ بازراستی‌آزمایی $\Rightarrow$ عیب‌یابیِ دوباره} -- (s3.west);

  % entry/exit criteria annotation
  \node[font=\footnotesize\itshape, align=right, text width=3.6cm, right=1.0cm of s7]
     {هر گام، معیارهای\\ورود/خروج دارد};

\end{tikzpicture}}
\end{document}
