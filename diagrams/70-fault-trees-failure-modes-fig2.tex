% Fault tree: TOP event "Unintended quench" decomposed via OR gates into three
% branches (coil heating, He level loss, external/mechanical excitation) and basic events.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    top/.style={rectangle, draw=black!75, very thick, fill=red!16,
      text width=4.0cm, align=center, inner sep=5pt, minimum height=1.0cm},
    inter/.style={rectangle, draw=black!70, thick, fill=orange!14,
      text width=2.7cm, align=center, inner sep=4pt, minimum height=1.0cm},
    basic/.style={circle, draw=black!70, thick, fill=yellow!22,
      align=center, inner sep=1pt, minimum size=1.35cm, font=\scriptsize},
    gate/.style={font=\footnotesize\bfseries, fill=white, inner sep=1.5pt},
    line/.style={-, thick, black!60}]

  \node[top] (top) {رویداد بالا (TOP):\\ \emph{کوئنچ ناخواسته}};
  \node[gate, below=0.30cm of top] (g0) {OR};

  % Three intermediate events
  \node[inter] (heat) at (-5.0,-2.3) {گرم‌شدن سیم‌پیچ\\ (گرمکن / نقطهٔ داغ)};
  \node[inter] (lvl)  at ( 0.0,-2.3) {افت سطح He زیر سیم‌پیچ‌ها};
  \node[inter] (mech) at ( 5.0,-2.3) {تحریک بیرونی / مکانیکی};

  \draw[line] (top) -- (g0);
  \draw[line] (g0) -- (heat);
  \draw[line] (g0) -- (lvl);
  \draw[line] (g0) -- (mech);

  % Branch 1: coil heating, OR
  \node[gate, below=0.25cm of heat] (g1) {OR};
  \node[basic] (ch) at (-6.1,-4.6) {توقف سرسرد (cold-head)};
  \node[basic] (rj) at (-3.9,-4.6) {اتصال مقاومتی};
  \draw[line] (heat) -- (g1);
  \draw[line] (g1) -- (ch);
  \draw[line] (g1) -- (rj);

  % Branch 2: He level, OR
  \node[gate, below=0.25cm of lvl] (g2) {OR};
  \node[basic] (leak) at (-1.1,-4.6) {نشت He};
  \node[basic] (hf)   at ( 1.1,-4.6) {خرابی گرمکن / هشدار};
  \draw[line] (lvl) -- (g2);
  \draw[line] (g2) -- (leak);
  \draw[line] (g2) -- (hf);

  % Branch 3: mechanical, OR
  \node[gate, below=0.25cm of mech] (g3) {OR};
  \node[basic] (proj) at (3.9,-4.6) {پرتابهٔ فرومغناطیس بزرگ};
  \node[basic] (vib)  at (6.1,-4.6) {ضربه / ارتعاش شدید};
  \draw[line] (mech) -- (g3);
  \draw[line] (g3) -- (proj);
  \draw[line] (g3) -- (vib);
\end{tikzpicture}}
\end{document}
