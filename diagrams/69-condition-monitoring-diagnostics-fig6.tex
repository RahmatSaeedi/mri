% Ishikawa / fishbone diagram for the symptom "helium boil-off trend",
% organized along six standard branches (6M).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    spine/.style={-{Stealth[length=4mm]}, very thick, black!75},
    bone/.style={thick, black!60},
    cat/.style={font=\small\bfseries, fill=blue!10, draw=black!50, rounded corners=2pt, inner sep=3pt},
    cause/.style={font=\footnotesize, align=center, black!75}]

  % main spine
  \coordinate (tail) at (0,0);
  \coordinate (head) at (12,0);
  \draw[spine] (tail) -- (head);

  % effect (fish head)
  \node[rectangle, rounded corners=3pt, draw=red!60!black, very thick, fill=red!10,
        text width=2.6cm, align=center, right=1pt of head] (effect)
        {\textbf{نشانه:}\\ روند تبخیر He};

  % upper branches: Machine, Method, Measurement
  \foreach \x/\cn/\ca in {2.4/ماشین/{نشت خلأِ\\ کرایوستات}, 5.6/روش/{برنامهٔ نامناسبِ\\ کلگی سرد}, 8.8/اندازه‌گیری/{رانشِ حسگرِ\\ سطح He}}{
    \draw[bone] (\x,0) -- (\x+1.6,2.3);
    \node[cat, anchor=south] at (\x+1.6,2.35) {\cn};
    \node[cause, anchor=east] at (\x+1.45,1.55) {\ca};
  }

  % lower branches: Material, Environment, Manpower
  \foreach \x/\cn/\ca in {2.4/مواد/{آلودگیِ جاذبِ\\ He}, 5.6/محیط/{دمای بالای\\ اتاق فنی}, 8.8/نیروی انسانی/{مرحلهٔ پرکردن /\\ سرویسِ ازقلم‌افتاده}}{
    \draw[bone] (\x,0) -- (\x+1.6,-2.3);
    \node[cat, anchor=north] at (\x+1.6,-2.35) {\cn};
    \node[cause, anchor=east] at (\x+1.45,-1.55) {\ca};
  }
\end{tikzpicture}}
\end{document}
