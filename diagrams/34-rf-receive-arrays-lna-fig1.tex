% Block diagram of a 32-channel RF receive array, from sample to digital receiver.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{amsmath}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    block/.style={draw, rounded corners=2pt, align=center, minimum width=42mm, minimum height=11mm, fill=blue!4},
    note/.style={font=\footnotesize, align=left, text=black!65}]

  % --- Sample / patient with array elements ---
  \node[draw, rounded corners=3pt, minimum width=78mm, minimum height=15mm, fill=red!4] (sample) {};
  \node[note, anchor=north west] at ([xshift=1mm,yshift=-1mm]sample.north west)
       {نمونه (بیمار) -- عناصرِ هم‌پوشانِ روی شاسیِ انعطاف‌پذیر/صلب};
  \foreach \x in {-2.6,-1.8,-1.0,-0.2} {
    \fill[blue!60!black] ([xshift=\x cm, yshift=-2.2mm]sample.center) circle (1.6pt);
  }
  \foreach \x in {-2.6,-1.8,-1.0,-0.2} {
    \fill[blue!60!black] ([xshift=\x cm, yshift=-4.6mm]sample.center) circle (1.6pt);
  }
  \node[note, anchor=west] at ([xshift=4mm,yshift=-3.4mm]sample.center)
       {هر $\bullet$ = یک حلقهٔ تشدید};

  % --- Element circuit ---
  \node[block, below=12mm of sample] (elem) {مدارِ عنصر\\$L_{\text{loop}}+C_t/C_m$};
  \node[note, anchor=west] at ([xshift=4mm]elem.east)
       {$C_t$ (کوک), $C_m$ (تطبیق)،\\شبکهٔ ناکوک‌سازیِ فعال\\$+$ تلهٔ حفاظتیِ غیرفعال};

  % --- LNA ---
  \node[block, below=14mm of elem] (lna) {LNA (با NF پایین)};
  \node[note, anchor=west] at ([xshift=4mm]lna.east)
       {$Z_{\text{in}}$ پایین $\Rightarrow$ واهم‌بندیِ پیش‌تقویت‌کننده};

  % --- Digital receiver ---
  \node[block, below=14mm of lna, minimum height=13mm] (rx) {گیرندهٔ دیجیتال\\۳۲ کانالِ موازی};
  \node[note, anchor=west] at ([xshift=4mm]rx.east)
       {پایین‌تبدیل $\to$ ADC};

  % --- Arrows ---
  \draw[->, thick] (sample.south) -- (elem.north);
  \draw[->, thick] (elem.south) -- node[right, font=\footnotesize, align=left]
       {شبکهٔ $\lambda/4$ یا\\تبدیلِ امپدانس} (lna.north);
  \draw[->, thick] (lna.south) -- node[right, font=\footnotesize, align=left]
       {coax $+$ تلهٔ کابل (balun)\\\RL{($\times\,32$ کانال)}} (rx.north);

\end{tikzpicture}}
\end{document}
