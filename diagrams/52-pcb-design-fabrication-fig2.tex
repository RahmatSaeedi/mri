% Hybrid grounding strategy on an MRI receiver board: continuous RF ground
% plane (multi-point) plus a single-point (star) connection for sensitive
% analog DC bias.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    blk/.style={draw, thick, rounded corners=1pt, fill=blue!8, minimum height=8mm, minimum width=15mm, align=center},
    via/.style={circle, draw, fill=gray!50, inner sep=1.4pt}]

  % continuous RF ground plane outline
  \fill[green!10] (0,0) rectangle (13,7);
  \draw[very thick, green!45!black] (0,0) rectangle (13,7);
  \node[anchor=north west, green!45!black, font=\bfseries\footnotesize] at (0.15,6.95)
    {صفحهٔ زمین RF (پیوسته، چندنقطه‌ای)};

  % RF chain blocks
  \node[blk] (lna)  at (1.7,5.0) {LNA};
  \node[blk] (filt) at (4.5,5.0) {فیلتر};
  \node[blk] (mix)  at (7.3,5.0) {آمیزنده};
  \node[blk] (line) at (10.6,5.0) {خط 50\,$\Omega$};

  \draw[->,thick] (lna)  -- (filt);
  \draw[->,thick] (filt) -- (mix);
  \draw[->,thick] (mix)  -- (line);
  \draw[->,thick] (line.east) -- ++(1.4,0) node[anchor=west,font=\scriptsize]{خروجی};
  \draw[<-,thick] (lna.west) -- ++(-1.3,0) node[anchor=east,font=\scriptsize]{ورودی RX};

  % short vias to plane under each RF block
  \foreach \n in {lna,filt,mix,line}{
    \node[via] (v\n) at ($(\n.south)+(0,-0.55)$) {};
    \draw[thick] (\n.south) -- (v\n);
  }
  \node[anchor=north, font=\scriptsize, green!45!black] at (4.5,3.85)
    {زمین RF چندنقطه‌ای: کوتاه‌ترین via به صفحه};

  % analog DC (bias) island
  \draw[very thick, orange!80!black, dashed] (1.2,0.7) rectangle (6.0,3.0);
  \node[anchor=north west, orange!80!black, font=\bfseries\footnotesize] at (1.3,2.95)
    {جزیرهٔ آنالوگ DC (بایاس)};
  \node[align=center, font=\scriptsize] at (3.6,1.9)
    {ترک‌های زمینِ\\ تک‌نقطه‌ای اینجا گرد می‌آیند};

  % star point: one via connecting island ground to RF plane
  \node[via, fill=red, inner sep=2pt] (star) at (3.6,0.7) {};
  \draw[thick, red!70!black] (2.4,1.3) -- (star);
  \draw[thick, red!70!black] (4.8,1.3) -- (star);
  \draw[thick, red!70!black] (3.6,1.3) -- (star);
  \node[anchor=north, red!70!black, font=\scriptsize, align=center] at (3.6,0.55)
    {نقطهٔ ستاره: یک via\\ به صفحهٔ RF};

\end{tikzpicture}}
\end{document}
