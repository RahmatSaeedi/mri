% Hybrid FPGA+GPU real-time recon: 32-ch receiver+ADC -> FPGA near ADC (DDC, FIR/CIC
% decimation, DC removal, channel compression; low-latency, deterministic) ->
% host+GPU (frame assembly, correction, NUFFT/FFT, SENSE/GRAPPA, deep net; high throughput)
% -> real-time display + scan-plane control feedback loop to console.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    blk/.style={rounded corners=4pt, draw=black, very thick, align=left, inner sep=7pt}]

  \node[blk, fill=blue!12, align=center] (rx) at (0,0)
    {\textbf{آرایهٔ گیرنده}\\ 32 کانال\\ $+$ ADC};

  \node[blk, fill=cyan!14, right=1.3cm of rx] (fpga)
    {\textbf{FPGA (نزدیک ADC)}\\
     \footnotesize $\bullet$ DDC، کاهش‌نرخِ FIR/CIC\\
     \footnotesize $\bullet$ حذف DC\\
     \footnotesize $\bullet$ فشرده‌سازی کانال‌ها\\[2pt]
     \footnotesize\itshape نهفتگی پایین، قطعی};

  \node[blk, fill=green!14, right=1.3cm of fpga] (gpu)
    {\textbf{میزبان $+$ GPU}\\
     \footnotesize $\bullet$ مونتاژ فریم، تصحیح\\
     \footnotesize $\bullet$ NUFFT / FFT\\
     \footnotesize $\bullet$ SENSE / GRAPPA\\
     \footnotesize $\bullet$ شبکهٔ عصبیِ ژرف\\[2pt]
     \footnotesize\itshape توان عملیاتی بالا، انعطاف‌پذیر};

  \draw[->, very thick] (rx)  -- (fpga);
  \draw[->, very thick] (fpga) -- (gpu);

  % display + feedback loop
  \node[blk, fill=gray!14, align=center, below=1.4cm of gpu] (disp)
    {نمایش بی‌درنگ\\ $+$ کنترل صفحهٔ اسکن};

  \draw[->, very thick] (gpu) -- (disp);

  % feedback arrow back to console (drawn under/around to the left)
  \draw[->, very thick, red!70!black]
    (disp.west) .. controls +(-3.5,0) and +(-1.8,-1.6) .. ($(fpga.south)+(0,-0.05)$)
    node[pos=0.55, below, font=\footnotesize, black, align=center]{حلقهٔ بازخورد به کنسول};

\end{tikzpicture}}
\end{document}
