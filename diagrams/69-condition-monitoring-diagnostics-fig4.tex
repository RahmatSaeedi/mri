% Bathtub curve: hazard rate h(t) across the three life phases
% (infant mortality, useful life, wear-out).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small]
  \begin{axis}[
      width=13cm, height=8cm,
      axis lines=left,
      xlabel={$t$ (سن)}, ylabel={$h(t)$ (نرخ مخاطره)},
      xmin=0, xmax=10, ymin=0, ymax=3.2,
      xtick=\empty, ytick=\empty,
      clip=false,
      every axis x label/.style={at={(ticklabel* cs:1.0)}, anchor=west},
      every axis y label/.style={at={(ticklabel* cs:1.0)}, anchor=south east},
    ]
    % bathtub = decreasing (infant) + constant + increasing (wear-out)
    % h(t) = 2.2*exp(-1.1*t) + 0.35 + 0.012*(max(0,t-5))^2.4
    \addplot[domain=0.05:9.6, samples=200, very thick, blue!65!black]
      {2.2*exp(-1.1*x) + 0.35 + 0.012*(max(0,x-5))^2.4};

    % phase boundaries
    \draw[gray, densely dashed] (axis cs:2.2,0) -- (axis cs:2.2,3.0);
    \draw[gray, densely dashed] (axis cs:5.0,0) -- (axis cs:5.0,3.0);

    % phase labels (top)
    \node[align=center, blue!50!black] at (axis cs:1.1,2.55)
      {مرگ‌ومیر\\زودرس};
    \node[align=center, black!70] at (axis cs:3.6,2.55)
      {عمر مفید\\\RL{(ثابت $\sim$ تصادفی)}};
    \node[align=center, red!55!black] at (axis cs:7.6,2.55)
      {فرسایش};

    % bottom-axis phase names
    \node[anchor=north, font=\footnotesize] at (axis cs:1.1,-0.08) {دورهٔ سوزاندن};
    \node[anchor=north, font=\footnotesize] at (axis cs:3.6,-0.08) {عمر مفید};
    \node[anchor=north, font=\footnotesize] at (axis cs:7.6,-0.08) {فرسایش};
  \end{axis}
\end{tikzpicture}}
\end{document}
