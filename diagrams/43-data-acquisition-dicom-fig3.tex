% Scatter-gather DMA: engine walks a circular descriptor ring, moving FIFO samples into scattered host pages.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    box/.style={draw, rounded corners=2pt, align=center},
    page/.style={draw, minimum width=10mm, minimum height=9mm, fill=orange!12, font=\footnotesize}]

  % FPGA FIFO source
  \node[box, fill=blue!8, minimum height=14mm, minimum width=20mm] (fifo) {FPGA FIFO\\(نمونه‌ها)};

  % Host memory region with scattered physical pages
  \node[box, fill=green!7, minimum width=58mm, minimum height=20mm, right=42mm of fifo] (host) {};
  \node[anchor=north, font=\footnotesize] at (host.north) {حافظهٔ میزبان (قابل‌دسترسِ DMA)};
  \node[page] (p7) at ($(host.center)+(-21mm,-2mm)$) {pg7};
  \node[page, right=2mm of p7] (p2) {pg2};
  \node[page, right=2mm of p2] (p9) {pg9};
  \node[page, right=2mm of p9] (p4) {pg4};
  \node[right=2mm of p4, font=\footnotesize] {$\cdots$};

  % DMA engine arrow
  \draw[->, very thick] (fifo.east) -- node[above, font=\footnotesize] {موتور DMA} (host.west);

  % Descriptor ring (below the FIFO)
  \node[box, fill=gray!10, align=center, below=16mm of fifo, minimum width=40mm] (ring)
       {حلقهٔ توصیف‌گرها\\$\mathrm{desc}_0 \to \mathrm{desc}_1 \to \cdots$\\
        \footnotesize هر توصیف‌گر: \{addr, len, flags (IRQ on done)\}};
  % ring-back loop
  \draw[->, thick, gray] (ring.east) .. controls +(14mm,0) and +(14mm,-10mm) .. (ring.north east)
        node[midway, right, font=\scriptsize] {پیچش به ابتدا};

  % descriptors point to scattered pages
  \draw[->, dashed] (ring.north) -- node[left, font=\scriptsize] {پیمایش حلقوی} (fifo.south);
  \draw[->, dashed, gray] (ring.north east) to[out=70, in=255] (host.south);

\end{tikzpicture}}
\end{document}
