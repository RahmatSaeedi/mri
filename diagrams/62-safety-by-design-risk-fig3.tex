% Fault tree for top event "Patient RF burn" with AND/OR gates.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    top/.style={draw, rectangle, rounded corners=2pt, fill=red!12, align=center,
                inner sep=4pt, text width=3.0cm, minimum height=1.0cm},
    inter/.style={draw, rectangle, fill=orange!14, align=center,
                inner sep=4pt, text width=2.7cm, minimum height=1.0cm},
    basic/.style={draw, circle, fill=blue!8, align=center, inner sep=1pt,
                text width=2.1cm, minimum size=1.9cm, font=\scriptsize},
    line/.style={-, thick},
  ]

  % Gate label style: little tag near a node
  \tikzset{gate/.style={font=\footnotesize\bfseries, fill=white, inner sep=1pt}}

  % Top event
  \node[top] (top) at (0,0) {رویداد رأس:\\ سوختگی RF بیمار};

  % Intermediate events (OR under top)
  \node[inter] (sar)  at (-6.0,-2.6) {SAR کل‌بدن بیش از حد مجاز};
  \node[inter] (hot)  at (0,-2.6)    {نقطهٔ داغ موضعی};
  \node[inter] (loop) at (6.0,-2.6)  {تماس حلقهٔ رسانا با پوست};

  % OR gate marker under top
  \node[gate] at (0,-1.05) {OR};
  \draw[line] (top.south) -- (0,-1.4);
  \draw[line] (0,-1.4) -- (sar.north);
  \draw[line] (0,-1.4) -- (hot.north);
  \draw[line] (0,-1.4) -- (loop.north);

  % Under SAR: AND gate -> two basic events
  \node[basic] (sarcalc) at (-7.2,-5.6) {پیش‌بینی نادرست SAR (وزن نادرست بیمار)};
  \node[basic] (sarmon)  at (-4.8,-5.6) {خرابی پایش‌گر بی‌درنگ توان};
  \node[gate] at (-6.0,-3.95) {AND};
  \draw[line] (sar.south) -- (-6.0,-4.3);
  \draw[line] (-6.0,-4.3) -- (sarcalc.north);
  \draw[line] (-6.0,-4.3) -- (sarmon.north);

  % Under hot-spot: OR gate -> two basic events
  \node[basic] (diel) at (-1.2,-5.6) {کوپلاژ دی‌الکتریک موضعی};
  \node[basic] (cable) at (1.2,-5.6) {مسیریابی نادرست کابل/سیم};
  \node[gate] at (0,-3.95) {OR};
  \draw[line] (hot.south) -- (0,-4.3);
  \draw[line] (0,-4.3) -- (diel.north);
  \draw[line] (0,-4.3) -- (cable.north);

  % Under loop: single basic event (training/setup error)
  \node[basic] (train) at (6.0,-5.6) {خطای آموزش/راه‌اندازی: کابل ECG روی پوست};
  \draw[line] (loop.south) -- (train.north);

\end{tikzpicture}}
\end{document}
