% System-level SNR chain: each stage can degrade SNR.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    block/.style={draw, rounded corners=2pt, align=center,
                  minimum width=2.1cm, minimum height=1.5cm, fill=blue!6, thick},
    note/.style={align=center, font=\scriptsize, text=red!60!black}]

  % chain of stages
  \node[block] (b0)    {$B_0$،\\ قطبش\\ $M_0$};
  \node[block, right=0.9cm of b0]   (coil)  {سیم‌پیچ\\ گیرنده\\ \RL{($Q$ بارگذاری‌شده)}};
  \node[block, right=0.9cm of coil] (lna)   {پیش‌تقویت‌کنندهٔ\\ کم‌نوفه\\ (NF)};
  \node[block, right=0.9cm of lna]  (rx)    {زنجیرهٔ\\ گیرندهٔ\\ دیجیتال};
  \node[block, right=0.9cm of rx]   (recon) {بازسازی\\ و فیلتر};

  % arrows
  \draw[->, thick] (b0)   -- (coil);
  \draw[->, thick] (coil) -- (lna);
  \draw[->, thick] (lna)  -- (rx);
  \draw[->, thick] (rx)   -- (recon);

  % degradation notes below each stage
  \node[note, below=0.35cm of b0]    {$\uparrow$ میدان};
  \node[note, below=0.35cm of coil]  {$\uparrow$ تطبیق امپدانس};
  \node[note, below=0.35cm of lna]   {$\uparrow$ NF $<0.5$\,dB};
  \node[note, below=0.35cm of rx]    {$\uparrow$ کوانتش /\\ لرزش};
  \node[note, below=0.35cm of recon] {$\uparrow$ پنجره‌گذاری};

  % title bar
  \node[align=center, font=\small\bfseries] at ($(b0.north)!0.5!(recon.north)+(0,0.85)$)
    {زنجیرهٔ SNR در سطح سامانه --- هر حلقه می‌تواند SNR را بکاهد};

\end{tikzpicture}}
\end{document}
