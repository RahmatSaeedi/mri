\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
  block/.style={draw, rounded corners=2pt, minimum width=34mm, minimum height=8mm, align=center, fill=blue!6},
  pa/.style={draw, rounded corners=2pt, minimum width=18mm, minimum height=8mm, align=center, fill=orange!15},
  node distance=6mm,
  flow/.style={->, thick}]

  % Main spine
  \node[block] (console) {DDS کنسول / باندپایه\\\scriptsize (mV، شکل پالس دیجیتال)};
  \node[block, below=of console] (mod) {فراتبدیل‌گر / مدولاتور\\\scriptsize RF در $f_0$ \LR{($\approx128$ MHz)}};
  \node[block, below=of mod] (drv) {پیش‌راننده / راننده\\\scriptsize کلاس A/AB، $\sim$40--50 dB، چند W};
  \node[block, below=of drv] (split) {تقسیم‌گر\\\scriptsize ویلکینسون / هیبرید 90$^\circ$};

  \draw[flow] (console) -- node[right]{\scriptsize I/Q یا پوش} (mod);
  \draw[flow] (mod) -- node[right]{\scriptsize $\sim0$ dBm} (drv);
  \draw[flow] (drv) -- (split);

  % PA modules row
  \node[pa, below left=10mm and 9mm of split] (pa1) {ماژول PA\\\scriptsize LDMOS AB};
  \node[pa, right=4mm of pa1] (pa2) {ماژول PA};
  \node[pa, right=4mm of pa2] (pa3) {ماژول PA};
  \node[right=4mm of pa3] (padots) {$\cdots$};

  \draw[flow] (split.south) -- (pa1.north);
  \draw[flow] (split.south) -- (pa2.north);
  \draw[flow] (split.south) -- (pa3.north);

  % Combiner
  \node[block, below=12mm of pa2, minimum width=44mm, fill=orange!8] (comb) {ترکیب‌گر (ویلکینسون / هیبرید)\\\scriptsize هم‌فاز، درگاه‌های ایزوله};
  \draw[flow] (pa1.south) -- (comb.north);
  \draw[flow] (pa2.south) -- (comb.north);
  \draw[flow] (pa3.south) -- (comb.north);

  \node[block, below=of comb] (filt) {فیلتر پایین‌گذر / هارمونیک\\\scriptsize سرکوب $2f_0,\ 3f_0$};
  \node[block, below=of filt] (coup) {جفت‌گر جهتی};
  \node[block, below=of coup] (trsw) {کلید T/R\\\scriptsize $\lambda/4$ + دیودهای PIN};

  \draw[flow] (comb) -- (filt);
  \draw[flow] (filt) -- (coup);
  \draw[flow] (coup) -- (trsw);

  % Coupler branch to protection
  \node[draw, dashed, rounded corners=2pt, right=14mm of coup, minimum height=8mm, align=center, fill=red!8] (prot) {آشکارساز توان\\رفت/برگشت $\rightarrow$ حفاظت};
  \draw[flow] (coup.east) -- (prot.west);

  % TX and RX branches
  \node[block, below left=10mm and -10mm of trsw, fill=green!10] (coil) {تطبیق / Balun\\$\rightarrow$ سیم‌پیچ Tx};
  \node[block, below right=10mm and -10mm of trsw, fill=green!10] (rx) {پیش‌تقویت‌کننده $\rightarrow$\\زنجیره Rx};

  \draw[flow] (trsw.south) -| node[pos=0.25, above]{\scriptsize TX} (coil.north);
  \draw[flow] (trsw.south) -| node[pos=0.25, above]{\scriptsize RX} (rx.north);

\end{tikzpicture}}
\end{document}
