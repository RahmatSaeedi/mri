% Standalone TikZ figure: routine brain MRI protocol as an ordered tree.
% Each series is a specialised "sensor" for one tissue property.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    seqbox/.style={draw, rounded corners=2pt, align=left, fill=blue!6, thick,
                  minimum height=8mm, text width=42mm, inner sep=3pt},
    lbl/.style={font=\footnotesize, text=black!60, align=left, text width=58mm},
    rootbox/.style={draw, rounded corners=2pt, align=center, fill=blue!16, thick,
                 minimum height=9mm, text width=50mm}]

  % root
  \node[rootbox] (root) at (0,0) {\textbf{پروتکل: MRI روتین مغز}\\ $({\sim}25\,\mathrm{min})$};

  % steps stacked below
  \node[seqbox] (s1) at (1.2,-1.6)  {۱. لوکالایزر (۳ صفحه)};
  \node[seqbox] (s2) at (1.2,-2.9)  {۲. T1w ساژیتال (FSE / 3D)};
  \node[seqbox] (s3) at (1.2,-4.2)  {۳. T2w اگزیال FSE};
  \node[seqbox] (s4) at (1.2,-5.5)  {۴. T2-FLAIR اگزیال};
  \node[seqbox] (s5) at (1.2,-6.8)  {۵. DWI اگزیال + نقشه ADC};
  \node[seqbox] (s6) at (1.2,-8.1)  {۶. SWI / $T_2^{*}$ GRE اگزیال};
  \node[seqbox] (s7) at (1.2,-9.8)  {۷. T1w پس از تزریق حاجب\\ \quad (پس از تزریق Gd)};

  % notes to the right of each step
  \node[lbl, right=4mm of s1] {تعیین موقعیت، چند ثانیه};
  \node[lbl, right=4mm of s2] {آناتومی، خط میانی، ساختار};
  \node[lbl, right=4mm of s3] {آب/ادم پرسیگنال؛ نقشه آسیب};
  \node[lbl, right=4mm of s4] {ضایعات ماده سفید؛ CSF تیره};
  \node[lbl, right=4mm of s5] {سکته حاد، آبسه};
  \node[lbl, right=4mm of s6] {خونریزی، کلسیفیکاسیون};
  \node[lbl, right=4mm of s7] {تخریب سد خونی-مغزی: تومور، عفونت،\\ التهاب (در صورت درخواست حاجب)};

  % vertical spine + branch connectors
  \coordinate (top) at (0,-0.55);
  \draw[thick] (top) -- (0,-9.8);
  \foreach \s in {s1,s2,s3,s4,s5,s6,s7}{
    \draw[thick] (0,0 |- \s.west) -- (\s.west);
  }

\end{tikzpicture}}
\end{document}
