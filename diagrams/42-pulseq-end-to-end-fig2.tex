% Sequence interpreter data flow: the .seq sections are parsed, shapes are
% decompressed (cumsum), resampled onto the hardware raster and converted from
% Hz/m to DAC counts with group-delay compensation, then handed to the FPGA
% firmware as a deterministic, time-stamped event queue.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    sec/.style={draw, rounded corners=1pt, align=left, fill=gray!8,
                font=\ttfamily\footnotesize, minimum width=20mm, inner sep=3pt},
    proc/.style={draw, rounded corners=3pt, align=left, fill=blue!5,
                 inner sep=5pt, minimum height=24mm},
    evt/.style={draw, align=left, fill=green!6, inner sep=5pt, minimum height=24mm},
    flow/.style={-{Stealth[length=2.6mm]}, thick}]

  %================= LEFT: .seq file sections =================
  \node[sec] (b1) at (0,2.4) {[BLOCKS]};
  \node[sec] (b2) at (0,1.6) {[SHAPES]};
  \node[sec] (b3) at (0,0.8) {[RF]/...};
  \node[draw, dashed, rounded corners=3pt, fit=(b1)(b2)(b3), inner sep=4pt] (seqbox) {};
  \node[font=\bfseries, align=center] at (0,3.5) {فایل \texttt{.seq}};

  %================= MIDDLE: interpreter (host) =================
  \node[proc] (interp) at (5.0,1.6) {
    واکشایی شکل (cumsum)\\[2pt]
    بازنمونه‌برداری روی شبکهٔ سخت‌افزار\\[2pt]
    Hz/m $\to$ شمارش‌های DAC\\[2pt]
    $+$ جبران تأخیر گروهی\\$(\tau_g,\tau_{\mathrm{rf}},\tau_{\mathrm{rx}})$};
  \node[font=\bfseries, align=center] at (5.0,3.5) {مفسّر (میزبان)};

  %================= RIGHT: FPGA firmware (real-time) =================
  \node[evt] (fpga) at (10.6,1.6) {
    \textbf{صف رویدادهای زمان‌مهرخورده}\\[3pt]
    \texttt{t=0}\hphantom{ms} : RF LUT $\leftarrow$ پوش پالس\\
    \texttt{t=0}\hphantom{ms} : GZ DAC $\leftarrow$ بالاروی\\
    \texttt{t=2ms} : ADC trigger $\leftarrow$ باز\\[3pt]
    \itshape ... کلاک قطعی 122.88\,MHz};
  \node[font=\bfseries, align=center] at (10.6,3.5) {میان‌افزار FPGA\\\footnotesize\mdseries (بی‌درنگ)};

  %================= FLOW ARROWS =================
  \draw[flow] (seqbox.east) -- node[above,font=\footnotesize]{تجزیه} (interp.west);
  \draw[flow] (interp.east) -- node[above,font=\footnotesize]{بایت‌ها} (fpga.west);

\end{tikzpicture}}
\end{document}
