% Anatomy of a formal, pre-approved test protocol (TP-SNR-01).
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    sec/.style={draw, rounded corners=1pt, align=left, text width=58mm,
                minimum height=7mm, inner sep=2pt, fill=blue!6}]

  % header bar
  \node[draw, fill=blue!22, rounded corners=2pt, text width=124mm,
        align=center, minimum height=9mm] (head)
        {\textbf{پروتکل آزمون\quad TP-SNR-01\quad rev B}\quad
         \fbox{\small تأییدشده $\triangleright$ تاریخ / امضا}};

  % two columns of numbered sections
  \node[sec, below left=4mm and 1mm of head.south] (s1)
        {\textbf{۱. هدف} --- راستی‌آزمایی REQ-IMG-014 \LR{(SNR $\geq 150$)}};
  \node[sec, below=2mm of s1] (s2)
        {\textbf{۲. دامنه} --- کل سامانه، مبنای T1-SE};
  \node[sec, below=2mm of s2] (s3)
        {\textbf{۳. مراجع} --- NEMA MS 1-2008؛ SOP-PHANTOM-03};
  \node[sec, below=2mm of s3] (s4)
        {\textbf{۴. تعاریف و اختصارات}};
  \node[sec, below=2mm of s4] (s5)
        {\textbf{۵. نقش‌ها} --- بهره‌بردار، شاهد، تأییدکننده};
  \node[sec, below=2mm of s5] (s6)
        {\textbf{۶. پیش‌شرط‌ها} --- $20\pm2^{\circ}$C، ۳۰\,دقیقه گرم‌شدن، فانتوم ACR در ایزوسنتر، کالیبراسیون معتبر};

  \node[sec, below right=4mm and 1mm of head.south] (s7)
        {\textbf{۷. تجهیزات و کالیبراسیون} --- شناسهٔ دارایی + انقضای کالیبراسیون};
  \node[sec, below=2mm of s7] (s8)
        {\textbf{۸. گام‌های آزمون} --- شماره‌گذاری‌شده، تکرارپذیر};
  \node[sec, below=2mm of s8] (s9)
        {\textbf{۹. داده‌های ثبت‌شده} --- جدول خام خالی};
  \node[sec, below=2mm of s9] (s10)
        {\textbf{۱۰. پذیرش / رد} --- $\mathit{SNR}_{meas} \geq 159$ (نوار محافظ)};
  \node[sec, below=2mm of s10] (s11)
        {\textbf{۱۱. مدیریت انحراف} --- ارجاع به NCR / CAPA};
  \node[sec, below=2mm of s11] (s12)
        {\textbf{۱۲. پیوست‌ها} --- DICOM خام / تصاویر صفحه، محاسبات};

\end{tikzpicture}}
\end{document}
