% Magnet/cold-head health chain: compressor -> cold head -> liquid-He vessel,
% with the key observable carried along each link.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    box/.style={rectangle, rounded corners=3pt, draw=black!70, very thick,
      fill=blue!8, text width=3.2cm, align=center, minimum height=1.35cm, inner sep=5pt},
    side/.style={rectangle, rounded corners=3pt, draw=black!55, thick,
      fill=gray!8, text width=3.2cm, align=center, minimum height=1.2cm, inner sep=5pt},
    obs/.style={->, very thick, blue!55!black},
    lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt, align=center}]

  % main vertical chain (left column)
  \node[box] (comp) {کمپرسور He};
  \node[box, below=2.0cm of comp] (cold) {کلگی سرد};
  \node[box, below=2.0cm of cold] (vessel) {مخزن He مایع};

  % right column targets
  \node[side, right=3.4cm of comp] (hp) {خط He پرفشار};
  \node[side, right=3.4cm of cold] (shield) {سپر حرارتی + چگالنده};
  \node[side, right=3.4cm of vessel] (bath) {حمام آهنربا};

  % horizontal links with observables
  \draw[obs] (comp) -- node[lbl, above]{فشار / جریان / دما} (hp);
  \draw[obs] (cold) -- node[lbl, above]{دمای مرحلهٔ ۱ و ۲} (shield);
  \draw[obs] (vessel) -- node[lbl, above]{سطح، فشار،\\ نرخ تبخیر} (bath);

  % vertical flow down the chain
  \draw[obs] (comp) -- (cold);
  \draw[obs] (cold) -- (vessel);
  \draw[obs] (hp) -- (shield);
  \draw[obs] (shield) -- (bath);
\end{tikzpicture}}
\end{document}
