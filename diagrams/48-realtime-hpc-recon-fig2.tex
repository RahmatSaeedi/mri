% Triple-stream CUDA overlap (Gantt-style): three streams pipeline H2D / compute / D2H
% of consecutive frames so the three GPU engines stay busy simultaneously.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    h2d/.style={draw=black, fill=blue!18, minimum height=8mm, align=center, inner sep=2pt},
    comp/.style={draw=black, fill=green!22, minimum height=8mm, align=center, inner sep=2pt},
    d2h/.style={draw=black, fill=orange!25, minimum height=8mm, align=center, inner sep=2pt}]

  \def\u{1.6}   % time unit width (cm)
  \def\rh{1.0}  % row height

  % time axis
  \draw[->, thick] (-0.2,0.6) -- (10.2,0.6) node[right]{زمان};

  % --- Stream 0 (row at y=0) : starts at t=0
  \node[left] at (-0.3, 0) {جریان 0};
  \node[h2d,  minimum width=\u cm] (a0) at (0.5*\u,0) {H2D $f_0$};
  \node[comp, minimum width=2*\u cm, anchor=west] (b0) at (a0.east) {محاسبهٔ $f_0$};
  \node[d2h,  minimum width=\u cm, anchor=west] (c0) at (b0.east) {D2H $f_0$};

  % --- Stream 1 (row at y=-rh) : shifted by one unit
  \node[left] at (-0.3, -\rh) {جریان 1};
  \node[h2d,  minimum width=\u cm, anchor=west] (a1) at (\u, -\rh) {H2D $f_1$};
  \node[comp, minimum width=2*\u cm, anchor=west] (b1) at (a1.east) {محاسبهٔ $f_1$};
  \node[d2h,  minimum width=\u cm, anchor=west] (c1) at (b1.east) {D2H $f_1$};

  % --- Stream 2 (row at y=-2rh) : shifted by two units
  \node[left] at (-0.3, -2*\rh) {جریان 2};
  \node[h2d,  minimum width=\u cm, anchor=west] (a2) at (2*\u, -2*\rh) {H2D $f_2$};
  \node[comp, minimum width=2*\u cm, anchor=west] (b2) at (a2.east) {محاسبهٔ $f_2$};
  \node[d2h,  minimum width=\u cm, anchor=west] (c2) at (b2.east) {D2H $f_2$};

  % overlap window highlight
  \draw[densely dashed, red!70!black, thick] (2*\u,0.45) -- (2*\u,-2*\rh-0.5);
  \draw[densely dashed, red!70!black, thick] (3*\u,0.45) -- (3*\u,-2*\rh-0.5);
  \node[red!70!black, align=center, font=\footnotesize] at (2.5*\u, -2*\rh-0.95)
    {بازهٔ هم‌پوشانی: موتورهای کپی‌ورودی، محاسبه و کپی‌خروجی همگی مشغول};

  % legend
  \node[h2d,  minimum width=1.1cm] (lg1) at (7.6, 0.05) {H2D};
  \node[comp, minimum width=1.1cm, right=2pt of lg1] (lg2) {محاسبه};
  \node[d2h,  minimum width=1.1cm, right=2pt of lg2] (lg3) {D2H};

\end{tikzpicture}}
\end{document}
