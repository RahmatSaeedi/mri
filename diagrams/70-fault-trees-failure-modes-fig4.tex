% Fault tree: TOP event "Low SNR / no signal" (RF chain) decomposed via OR into
% transmit path, receive path, tune/match, then to basic events.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    top/.style={rectangle, draw=black!75, very thick, fill=red!14,
      text width=5.0cm, align=center, inner sep=5pt, minimum height=1.0cm},
    inter/.style={rectangle, draw=black!70, thick, fill=blue!10,
      text width=2.7cm, align=center, inner sep=4pt, minimum height=1.1cm},
    basic/.style={circle, draw=black!70, thick, fill=yellow!22,
      align=center, inner sep=1pt, minimum size=1.4cm, font=\scriptsize},
    gate/.style={font=\footnotesize\bfseries, fill=white, inner sep=1.5pt},
    line/.style={-, thick, black!60}]

  \node[top] (top) {رویداد بالا (TOP):\\ \emph{SNR پایین یا افت کامل سیگنال}};
  \node[gate, below=0.30cm of top] (g0) {OR};

  \node[inter] (tx) at (-5.2,-2.4) {مسیر فرستنده \RL{(بدون $B_1$)}};
  \node[inter] (rx) at ( 0.0,-2.4) {مسیر گیرنده (بدون دریافت)};
  \node[inter] (mt) at ( 5.2,-2.4) {تنظیم / تطبیق (ناتنظیمی)};

  \draw[line] (top) -- (g0);
  \draw[line] (g0) -- (tx);
  \draw[line] (g0) -- (rx);
  \draw[line] (g0) -- (mt);

  \node[gate, below=0.25cm of tx] (g1) {OR};
  \node[basic] (rfpa) at (-6.3,-4.9) {از کارافتادن RFPA};
  \node[basic] (trsw) at (-4.1,-4.9) {کلید T/R};
  \draw[line] (tx) -- (g1);
  \draw[line] (g1) -- (rfpa);
  \draw[line] (g1) -- (trsw);

  \node[gate, below=0.25cm of rx] (g2) {OR};
  \node[basic] (lna) at (-1.1,-4.9) {از کارافتادن LNA};
  \node[basic] (rcab) at (1.1,-4.9) {شل‌بودن کابل / رابط Rx};
  \draw[line] (rx) -- (g2);
  \draw[line] (g2) -- (lna);
  \draw[line] (g2) -- (rcab);

  \node[gate, below=0.25cm of mt] (g3) {OR};
  \node[basic] (match) at (4.1,-4.9) {تطبیق سیم‌پیچ};
  \node[basic] (load) at (6.3,-4.9) {بارگذاری نادرست بیمار};
  \draw[line] (mt) -- (g3);
  \draw[line] (g3) -- (match);
  \draw[line] (g3) -- (load);
\end{tikzpicture}}
\end{document}
