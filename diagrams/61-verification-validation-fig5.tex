% Nonconformance (NCR/CAPA) management workflow triggered by a failed test.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small, node distance=7mm,
    proc/.style={draw, rounded corners=2pt, align=center, text width=62mm,
                 minimum height=9mm, fill=blue!8},
    start/.style={draw, rounded corners=8pt, align=center, text width=46mm,
                  minimum height=9mm, fill=red!15},
    dec/.style={draw, diamond, aspect=2.2, align=center, inner sep=1pt,
                text width=24mm, fill=orange!15},
    flow/.style={->, very thick, blue!55!black}]

  \node[start] (fail) {آزمون \textbf{مردود می‌شود}};
  \node[proc, below=of fail] (ncr) {ثبت \textbf{NCR}\\(شناسه، شرح، دادهٔ خام، شدت)};
  \node[proc, below=of ncr] (cont) {\textbf{مهار}\\علامت‌گذاری سامانه به «نامناسب برای کاربرد بالینی»};
  \node[proc, below=of cont] (rca) {تحلیل ریشه‌ای علت (\textbf{RCA})\\5-Whys / Ishikawa / FTA};
  \node[dec, below=10mm of rca] (need) {CAPA\\لازم است؟};

  \node[proc, below=11mm of need] (capa) {تعریف اقدام \textbf{اصلاحی} (رفع علت)\\+ \textbf{پیشگیرانه} (توقف تکرار)};
  \node[proc, below=of capa] (impl) {اجرا + راستی‌آزمایی\\\textbf{بررسی اثربخشی}};
  \node[proc, below=of impl, fill=green!10] (close) {به‌روزرسانی RTM / مستندات + بستن CAPA\\+ اجرای مجدد آزمون};

  % side branch for "no"
  \node[proc, right=14mm of need, text width=40mm, fill=green!10] (quick)
        {رفع موضعی + بستن NCR};

  \draw[flow] (fail) -- (ncr);
  \draw[flow] (ncr) -- (cont);
  \draw[flow] (cont) -- (rca);
  \draw[flow] (rca) -- (need);
  \draw[flow] (need) -- node[left, pos=0.35]{\footnotesize بله} (capa);
  \draw[flow] (need) -- node[above]{\footnotesize خیر} (quick);
  \draw[flow] (capa) -- (impl);
  \draw[flow] (impl) -- (close);

\end{tikzpicture}}
\end{document}
