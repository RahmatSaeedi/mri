% Design-control lifecycle: where Verification and Validation close the loop.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small,
    box/.style={draw, rounded corners=2pt, minimum height=11mm, minimum width=27mm, align=center, fill=blue!8},
    flow/.style={->, very thick, blue!55!black}]

  % top row: forward design flow
  \node[box] (need)  {نیازهای کاربر /\\نیازهای بالینی};
  \node[box, right=14mm of need]  (in)   {ورودی\\طراحی};
  \node[box, right=14mm of in]    (out)  {خروجی\\طراحی};
  \node[box, right=14mm of out, fill=green!10] (dev) {دستگاه\\ساخته‌شده};

  \draw[flow] (need) -- (in);
  \draw[flow] (in)   -- (out);
  \draw[flow] (out)  -- (dev);

  % verification arc: output <- device (matches output to input)
  \draw[->, very thick, orange!75!black]
    (dev.south) .. controls +(0,-12mm) and +(0,-12mm) .. (out.south)
    node[midway, below=1mm, align=center, orange!60!black]
    {\textbf{راستی‌آزمایی}\\\textit{خروجی در برابر ورودی؟}};

  % validation arc: device -> need (meets the real need)
  \draw[->, very thick, red!70!black]
    (dev.north) .. controls +(0,16mm) and +(0,16mm) .. (need.north)
    node[midway, above=1mm, align=center, red!60!black]
    {\textbf{صحه‌گذاری}\\\textit{نیاز کاربر را برآورده می‌کند؟}};

\end{tikzpicture}}
\end{document}
