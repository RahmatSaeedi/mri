% Active-detuning circuit: a PIN-diode trap (L_det in series with a PIN diode)
% forms a parallel resonance with the loop capacitor C_t during transmit.
\documentclass{article}
\usepackage[paperwidth=120cm,paperheight=120cm,margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{fontspec}
\usepackage[bidi=basic]{babel}
\babelprovide[import]{english}
\babelprovide[main, import]{persian}
\babelfont{rm}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\babelfont{sf}[Renderer=Node, Path=../fonts/, Extension=.ttf, UprightFont=*-Regular, BoldFont=*-Bold]{Vazirmatn}
\providecommand{\setRTL}{}\providecommand{\setLTR}{}
\providecommand{\RL}[1]{\foreignlanguage{persian}{#1}}
\providecommand{\LR}[1]{\babelsublr{#1}}
\usepackage{amsmath}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, shapes.misc, calc, fit, backgrounds, decorations.pathmorphing, patterns, angles, quotes}
\begin{document}
\setRTL
\tikzset{every node/.append style={execute at begin node=\setRTL}}
\babelsublr{\begin{tikzpicture}[>=Stealth, font=\small, line width=0.6pt]

  % --- main signal node / top rail ---
  \coordinate (top) at (0,3.2);
  \coordinate (mid) at (0,2.0);
  \coordinate (bot) at (0,-1.6);

  % signal line to LNA
  \draw[thick] (top) -- (3.2,3.2);
  \node[right] at (3.2,3.2) {خطِ سیگنال به LNA};
  \draw[->, thick] (2.6,3.2) -- (3.2,3.2);

  % vertical spine
  \draw[thick] (top) -- (mid);

  % --- C_t capacitor (resonance cap of the loop) ---
  \draw[thick] (-0.5,2.0) -- (0.5,2.0);
  \draw[thick] (-0.5,1.82) -- (0.5,1.82);
  \node[right] at (0.6,1.9) {$C_t$ (خازنِ کوکِ حلقه)};
  \draw[thick] (mid) -- (0,1.82);
  \coordinate (below_ct) at (0,1.82);
  \draw[thick] (below_ct) -- (0,1.1);

  % branch point
  \coordinate (bp) at (0,1.1);
  \fill (bp) circle (1.4pt);

  % --- L_det inductor (drawn as coil) on the detune branch ---
  \draw[thick] (bp) -- (1.0,1.1);
  \draw[thick, decorate, decoration={coil, aspect=0.6, segment length=4pt, amplitude=4pt}]
       (1.0,1.1) -- (2.4,1.1);
  \node[above] at (1.7,1.35) {$L_{\text{det}}$};

  % --- PIN diode ---
  \draw[thick] (2.4,1.1) -- (2.9,1.1);
  \fill[black] (2.9,0.9) -- (2.9,1.3) -- (3.35,1.1) -- cycle;   % triangle
  \draw[thick] (3.35,0.9) -- (3.35,1.3);                        % bar
  \draw[thick] (3.35,1.1) -- (3.9,1.1);
  \node[above] at (3.4,1.35) {دیودِ PIN};

  % --- RF choke + bias to diode branch ---
  \draw[thick] (3.9,1.1) -- (3.9,0.3);
  \draw[thick, decorate, decoration={coil, aspect=0.6, segment length=3pt, amplitude=3pt}]
       (3.9,0.3) -- (3.9,-0.5);
  \node[right] at (4.05,-0.1) {چوکِ RF};
  \draw[thick] (3.9,-0.5) -- (3.9,-1.0);
  \node[right] at (3.95,-1.2) {$+\,I_{\text{bias}}$ \RL{(DC در فرستادن)}};

  % --- loop connection at bottom ---
  \draw[thick] (bp) -- (0,-1.0);
  \node[below] at (0,-1.0) {حلقه};
  \draw[thick] (-0.6,-1.0) -- (0.6,-1.0);

  % --- explanatory boxes ---
  \node[draw, rounded corners=2pt, align=left, fill=red!5, font=\footnotesize, text width=72mm]
       at (4.3,-2.4)
       {فرستادن: دیود روشن $\Rightarrow$ تشدیدِ موازیِ $L_{\text{det}}\!+\!C_t$
        \RL{($Z$ بالا)} $\Rightarrow$ حلقه ناکوک};
  \node[draw, rounded corners=2pt, align=left, fill=green!6, font=\footnotesize, text width=72mm]
       at (4.3,-3.4)
       {دریافت: دیود خاموش $\Rightarrow$ تله غیرفعال $\Rightarrow$ حلقه به‌طورِ عادی کوک};

\end{tikzpicture}}
\end{document}
